import Vue from './base_ext';
import uploadImage from '../compontent/UploadImage'
import '@css/person_reg';

new Vue({
    el: '#app',
    components: {
        uploadImage
    },

    data() {
        return {
        }
    }
});